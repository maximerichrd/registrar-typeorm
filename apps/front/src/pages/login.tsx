import { LoginScene } from '@demo/front/domains/login';
import type { LoginSceneProps } from '@demo/front/domains/login';

export default function LoginPage(props: LoginSceneProps) {
  return <LoginScene {...props} />;
}
